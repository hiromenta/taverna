export enum Themes {
    DEFAULT = 'default'
};

export interface Theme {
    white: string;
    black: string;
    primary: string;
    secondary: string;
    accent: string;
    light: string;
    dark: string;
    danger: string;
    success: string;
}